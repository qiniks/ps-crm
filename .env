# Dev uses SQLite for zero-config setup.
# For production, switch the provider in prisma/schema.prisma to "postgresql"
# and set DATABASE_URL to your Postgres connection string, e.g.:
# DATABASE_URL="postgresql://user:password@localhost:5432/ps_crm?schema=public"
DATABASE_URL="file:./dev.db"
